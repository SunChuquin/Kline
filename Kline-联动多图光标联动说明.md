# 联动多图模式 · 光标联动使用说明

> 适用版本：Kline（iPad）。本文说明在**联动多图模式**下，点击右上角「**联**」字按钮开启**光标联动**后，多个视图之间十字光标的联动规则与操作方式。

## 目录

- [1. 功能简介](#1-功能简介)
- [2. 前置概念：联动多图模式](#2-前置概念联动多图模式)
- [3. 开启与关闭光标联动](#3-开启与关闭光标联动)
- [4. 基本操作](#4-基本操作)
- [5. 联动核心规则](#5-联动核心规则)
- [6. 两种联动画面：十字光标 与 双竖轴范围框](#6-两种联动画面十字光标-与-双竖轴范围框)
- [7. 自动居中与自动缩放](#7-自动居中与自动缩放)
- [8. 发起权与操作优先级](#8-发起权与操作优先级)
- [9. 典型场景示例](#9-典型场景示例)
- [10. 联动期间各视图的独立操作](#10-联动期间各视图的独立操作)
- [11. 与其他功能的关系](#11-与其他功能的关系)
- [12. 光标清除时机一览](#12-光标清除时机一览)
- [13. 常见问题](#13-常见问题)
- [附：技术实现索引](#附技术实现索引)

---

## 1. 功能简介

光标联动开启后，在联动多图中的**任意一个视图**里点出或拖动十字光标，其余视图会**自动定位到同一个交易日**，实现多周期、多标的同屏对照：

- 其余视图与来源视图**周期相同或更大**（或周期相同、标的不同）：出现一根**十字光标**对准同一日期附近的 K 线，横线精确落在该 K 线的**收盘价**价位，并自动滚动居中；
- 其余视图周期**更小**（例如来源是周线、另一视图是日线）：不显示十字光标，改为用**两根蓝色竖轴 + 淡蓝底纹**框出来源那根大周期 K 线所覆盖的整段时间（周一～周五的全部日 K、整月的全部日 K/周 K……），并自动滚动、必要时自动缩小缩放级别让整段范围入屏。

同步只认**日期**（YYYYMMDD），不认价格：各视图光标的价格数值永远取自本视图自己的 K 线。

## 2. 前置概念：联动多图模式

- 单图状态下点击右上角「**联**」即进入联动多图模式：屏幕横向排布 **2 / 3 / 4 个视图**，每个视图可独立设置**标的**与**周期**（日线 / 周线 / 月线 / 季线 / 年线）。
- 首次对某标的使用时，默认是 **2 个视图：左日线、右周线**（同一标的）。视图数量在「⚙ K线设置 → 联动视图数量」中修改，配置按主标的分别记忆。
- 每个视图下方信息栏从左到右为：主图指标按钮、标的名称、标的代码、单视图重置按钮、最右侧的「钻取单图」按钮（›）。

> 注意：**进入联动多图 ≠ 光标已经联动**。刚进入时「联」字是灰色，各视图光标相互独立；需要再点一次「联」开启后才会同步（见下节）。

## 3. 开启与关闭光标联动

「联」字按钮在不同状态下的含义：

| 所处状态 | 「联」字颜色 | 点击行为 |
| --- | --- | --- |
| 普通单图 | 灰色 | 进入联动多图模式（光标联动默认**关闭**） |
| 联动多图 · 联动关闭 | 灰色 | **开启**光标联动（所有视图按日期同步） |
| 联动多图 · 联动开启 | **蓝色** | **关闭**光标联动，各视图恢复独立 |

说明：

- 开启或关闭的瞬间，屏幕上**所有视图的十字光标 / 范围框会被一次性清空**，需要重新在任一视图点出。这样可以保证开关切换时没有残留状态。
- 关闭联动后，各视图十字光标完全独立，互不影响；视图的标的、周期、缩放位置均不改变。
- 退出联动多图（点左上角返回）或从信息栏「›」钻取单图时，联动光标同样会清空；从钻取单图返回联动页时，之前的联动开关状态会自动恢复（但光标需重新点出）。

## 4. 基本操作

- **点出光标**：在任一视图的图表区域轻点，即在该位置放出十字光标，该视图即成为本次联动的**来源视图**，其余视图立刻跟随。
- **拖动光标**：手指按住后拖动，光标逐根 K 线移动，其余视图实时跟随（拖动超过轻微阈值才判定为拖动，避免误触）。
- **取消光标**：在来源视图已有光标的位置再点一下，光标消失，所有视图的联动光标 / 范围框同时消失。
- 联动多图下每个视图**最多只有一个十字光标**，且不提供单图模式的 📌 固定光标（双光标对比）功能。

## 5. 联动核心规则

1. **同步基准是交易日日期**：来源视图把当前光标所指 K 线的日期广播给所有视图，各视图按日期在自己的数据中定位。
2. **只同步时间，不同步价格**：被联动视图的开、收、高、低、涨跌幅及各副图指标数值，全部显示本视图自己那根 K 线的数据。
3. **周期大小决定画面形态**：
   - 目标视图周期 **≥ 来源周期**（含同周期）→ 显示**十字光标**；
   - 目标视图周期 **< 来源周期** → 显示**双竖轴范围框**。
4. **找不到同一天就取最近**：目标视图在该日期没有 K 线（停牌、不同标的、周/月线日期不重合等）时，自动定位到时间上**最接近的一根** K 线。
5. **来源视图自身不动**：您手指操作的那个视图窗口位置保持不变，只有被联动视图会自动滚动定位。
6. **每个视图都可以发起**：没有固定的"主视图"，在哪个视图直接操作，哪个视图就立即成为新的来源。

### 周期对照表

下表为"在某周期视图（行）触发光标后，其它周期视图（列）的反应"：

| 来源 ＼ 目标 | 日线 | 周线 | 月线 | 季线 | 年线 |
| --- | --- | --- | --- | --- | --- |
| **日线** | 十字光标 | 十字光标 | 十字光标 | 十字光标 | 十字光标 |
| **周线** | **范围框**（周一～周日） | 十字光标 | 十字光标 | 十字光标 | 十字光标 |
| **月线** | **范围框**（整月） | **范围框**（整月） | 十字光标 | 十字光标 | 十字光标 |
| **季线** | **范围框**（整季） | **范围框**（整季） | **范围框**（整季） | 十字光标 | 十字光标 |
| **年线** | **范围框**（整年） | **范围框**（整年） | **范围框**（整年） | **范围框**（整年） | 十字光标 |

> 同周期不同标的（如两个日线视图显示两只股票）一律走十字光标：同一天两只股票都有 K 线则精确对齐同一天；一方当日停牌 / 无数据则对齐到最近交易日。

## 6. 两种联动画面：十字光标 与 双竖轴范围框

### 6.1 十字光标（目标周期 ≥ 来源周期）

被联动视图上出现的十字光标包含：

- **竖线**：对准同一日期（或最近日期）的那根 K 线，贯穿主图与各副图；顶部有日期标签；
- **横线**：精确对准该根 K 线的**收盘价**价位，贯穿主图，左侧价格标签显示的就是该 K 线收盘价（开启多/空镜像时随镜像价格域取负，与镜像 K 线一致）——因为您的手指并不在这个视图里，横线不跟随手指，而是直接锚定收盘价，与底部行情行的"收"一致；
- **底部行情行**：显示该根 K 线的开 / 收 / 高 / 低 / 涨跌幅等（联动态不显示"额"）；
- **各副图数值栏**：同步显示该 K 线对应周期下的指标数值。

### 6.2 双竖轴范围框（目标周期 < 来源周期）

当来源是更大周期的一根 K 线时，它在小周期视图里对应多根 K 线，无法用单一光标表达，因此：

- 画面上出现**两根半透明蓝色竖轴**（无文字标签，贯穿主图与全部副图），两轴之间有**淡蓝色底纹**；
- 左轴 = 来源 K 线覆盖区间的**起始日**，右轴 = **结束日**（周线固定周一～周日，月/季/年线为整月/整季/整年，均含首尾）；
- 此状态下该视图**不显示十字光标**，也不显示单根 K 线行情，仅表达"来源这根大 K 线对应本视图的这一段"。

范围框不出现的情况（属于正常现象）：

- 该视图在覆盖区间内**一根 K 线都没有**（长期停牌、标的不同且无重叠行情）；
- 区间右端**超出本视图已有数据**（例如最新一根周 K 所在周尚未结束、本地日线数据未更新到周末）——为避免框出不存在的数据，此时不绘制范围框。

## 7. 自动居中与自动缩放

- **十字光标型**：每次联动到达（含持续拖动来源光标），被联动视图都会自动水平滚动，把目标 K 线放到屏幕**水平中央**；目标 K 线本就在窗口内时也会重新居中，以保证多图视线对齐。
- **范围框型**：自动滚动让**整段覆盖范围居中可见**；
  - 当前窗口能装下整个范围 → 保持当前缩放，只平移；
  - 装不下 → **自动放大可见 K 线数**（即缩小 K 线、展示更长区间），最大可放大到该视图全部 K 线，不会无限缩放。
- **双指缩放优先**：自动定位若发生在您正用双指缩放某视图时，会让位于您的手指、不强行打断；手势结束后后续联动照常居中。
- 自动滚动 / 缩放只改变显示位置，**不会修改任何视图持久化的缩放级别**；您自己双指调整的缩放级别仍按（标的 + 周期）分别记忆。

## 8. 发起权与操作优先级

- **任一视图都可发起**：在视图 A 点出光标，全部视图跟 A；随后直接在视图 B 上点击 / 拖动，B 立即接管为新来源，全部视图转为跟 B，无需先关闭任何东西。
- **本视图手指操作优先**：当您正在某个视图里用手指拖动光标时，该视图暂时忽略外来联动，避免光标被外部同步拽走、手指下一帧又拉回造成闪烁；手指松开后自动恢复接收。
- **回声自动过滤**：视图只会响应"别人"发起的联动，自己广播出去的日期不会再反过来驱动自己，不会出现来回弹跳。

## 9. 典型场景示例

**场景一：默认日线 + 周线双视图**

1. 进入个股详情 → 点「联」进入双视图（左日线、右周线）→ 再点「联」变蓝，开启联动；
2. 在左侧日线视图拖动十字光标浏览某几天的走势；
3. 右侧周线视图出现十字光标，始终落在该日所属（或最近）的那根周 K 上，并自动居中，可直接对照周线级别指标；
4. 反过来在右侧周线视图拖动某根周 K：左侧日线视图出现**两根蓝轴**，框出该周一～周五的全部日 K，窗口自动调整到整周入屏。

**场景二：4 视图跨周期全景**

设置 4 个视图为日 / 周 / 月 / 季（可同标的，也可通过副图二滑动或 🔍 搜索混入相关标的）。在季线视图点出光标后：日 / 周 / 月三个视图同时显示蓝色范围框（分别框出该季度内的日 K、周 K、月 K），年线视图显示十字光标落到最近的年 K——一次点击即可看到同一时间点在五个周期级别的位置。

**场景三：同周期多标的对照**

把两个视图都设为日线、分别选择两只标的。在任一视图拖动光标，另一视图的光标对齐同一交易日；遇到一方停牌无当日 K 线时，自动落在与之最近的交易日 K 线上。

## 10. 联动期间各视图的独立操作

光标联动只同步"看哪一天"，以下操作每个视图仍各自独立、互不影响：

- **切周期**：在上方副图左右滑动（左滑更小周期、右滑更大周期）；
- **切标的**：在下方副图左右滑动切换候选列表中的上 / 下一个标的，或点副图二指标栏右侧 **🔍** 搜索任意标的；
- **双指缩放 / 单指平移**：缩放级别按（标的 + 周期）分别持久记忆；
- **主副图指标、指标参数、公式编辑**：各视图独立配置。

切换某视图的周期或标的后，该视图**仍是联动成员**，联动开关状态不变；若此时正有联动光标，新视图会按同一日期继续标注，再移动一下来源光标即重新居中。

## 11. 与其他功能的关系

- **「边」分界线调节**：联动多图工具栏用「边」按钮替代了单图的 📌。开启后可拖动视图间分界线调整宽度比例（2 视图可调宽；3 / 4 视图等宽排布）；调节期间**禁止新建 / 拖动十字光标**，以免手势冲突。**开启「边」的瞬间会清空当前所有视图的十字光标 / 范围框（一次清场）**，关闭「边」后光标不会自动恢复，需要时重新在任一视图点出；联动开关本身保持开启。
- **📌 固定光标（双光标对比）**：仅单图模式提供；联动多图下不显示该按钮，每个视图最多一个光标。
- **钻取单图（信息栏最右 ›）**：把某一格临时放大为单图查看。进入钻取会清空联动光标，钻取期间不参与联动；点返回回到联动页，原联动开关 / 边线等状态自动恢复。
- **退出联动**：左上角返回先退出联动多图（自动关闭联动、清空光标），再点一次返回才关闭详情页。
- **重置视图配置**：信息栏每格的重置按钮（改过标的 / 周期后高亮）只恢复该格的默认标的与周期，不影响联动开关。

## 12. 光标清除时机一览

| 时机 | 所有视图联动光标 / 范围框 | 联动开关状态 |
| --- | --- | --- |
| 联动开启中，在来源视图再点一下取消光标 | 清除 | 保持开启 |
| 点「联」开启联动 | 先清空，再重新使用 | 变为开启（蓝） |
| 点「联」关闭联动 | 清除 | 变为关闭（灰） |
| 开启「边」边线调节 | **清除**（本地光标 + 联动光标 / 范围框一并清场） | 保持开启；关「边」后需重新点出光标 |
| 信息栏「›」钻取单图 | 清除 | 临时挂起，返回时恢复 |
| 左上角返回退出联动 | 清除 | 随联动一起退出 |

## 13. 常见问题

**Q：为什么我在周线视图点了光标，日线视图出现的是两根蓝线而不是十字光标？**
A：这是专门的"范围框"表达——一根周 K 对应周一到周五的多根日 K，单根光标无法表达这种一对多关系。蓝轴 + 淡蓝底纹框出的就是这根周 K 覆盖的全部日 K。

**Q：为什么有时候小周期视图连蓝轴也不显示？**
A：该视图在来源 K 线覆盖的时间段内没有任何 K 线（停牌、标的无重叠行情），或覆盖区间右端超出了该视图已有的数据（如本周尚未结束、数据未更新）。更新数据或选择已完结的历史 K 线即可正常显示。

**Q：被联动视图里十字光标的横线对准哪个价格？**
A：横线精确对准竖线所指 K 线的**收盘价**，左侧标签即收盘价数值，与底部行情行的"收"一致；竖线顶部日期、底部开 / 收 / 高 / 低及各副图数值也都取自同一根 K 线。您手指直接操作的来源视图里，横线仍跟随手指位置，不受影响。

**Q：拖动时各个窗口为什么会自己滑动？**
A：自动居中机制，用于让所有视图的同一时间点处于相近视线位置。您正在直接用手指操作的那个窗口不会被移动；双指缩放某视图时自动定位也会临时让路。

**Q：我在一个视图上拖动，会被其它视图的联动"抢"光标吗？**
A：不会。手指直接操作的视图优先级最高，拖动期间忽略外来联动；松手后才重新接收。想去看另一个视图的位置，直接在那个视图上点击 / 拖动即可，它会自动成为新的来源。

**Q：光标联动会同步各视图的缩放和窗口位置吗？**
A：不会。只同步日期；各视图缩放级别、平移位置独立并分别记忆。仅在需要让目标 K 线 / 范围入屏时，被联动视图会临时自动居中或放大可见区间。

---

## 附：技术实现索引

供开发维护参考，普通用户无需阅读：

- 联动同步对象（广播光标日期、来源周期、覆盖范围）：[KlineDetailView.swift](Kline/KlineDetailView.swift) 中的 `DualLinkSync`
- 「联」按钮开关状态、清光标广播令牌、钻取快照：`KlineDetailView.swift` 中的 `cursorLinkEnabled` / `cursorClearToken` / `drillInSnapshot`
- 光标的发布与接收（防回声、防拖动冲突、居中、范围框定位）：[KlineChartView.swift](Kline/KlineChartView.swift) 中的 `publishLinkCursor` / `applyLinkCursor` / `renderCursorIndex` / `linkRangeIndices` / `centerLinkRange`
- 周期粒度等级与大周期 K 线覆盖区间计算：[KlineData.swift](Kline/KlineData.swift) 中的 `KlinePeriod.granularityRank` / `KlinePeriod.periodDateRange`
- 范围框渲染（双蓝轴 + 淡蓝底纹）：`KlineChartView.swift` 中的 `linkRangeAxisOverlay`
- 联动视图数量 / 标的 / 周期配置与缩放持久化：[LinkedViewStore.swift](Kline/LinkedViewStore.swift)（`Documents/LinkedViews.json`、`Documents/LinkedZooms.json`）
- 单格视图的数据加载与切周期 / 切标的手势：[LinkedKlineTile.swift](Kline/LinkedKlineTile.swift)
