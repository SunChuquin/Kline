# 联动多图模式 · 光标联动使用说明

> 适用版本：Kline（iPad）。本文说明在**联动多图模式**下，点击右上角「**联**」字按钮开启**光标联动**后，多个视图之间十字光标的联动规则与操作方式。

## 目录

- [1. 功能简介](#1-功能简介)
- [2. 前置概念：联动多图模式](#2-前置概念联动多图模式)
- [3. 开启与关闭光标联动](#3-开启与关闭光标联动)
- [4. 基本操作](#4-基本操作)
- [5. 联动核心规则](#5-联动核心规则)
- [6. 三种联动画面：历史时点复盘、十字光标 与 双竖轴范围框](#6-三种联动画面历史时点复盘十字光标-与-双竖轴范围框)
- [7. 自动居中与自动缩放](#7-自动居中与自动缩放)
- [8. 发起权与操作优先级](#8-发起权与操作优先级)
- [9. 典型场景示例](#9-典型场景示例)
- [10. 联动期间各视图的独立操作](#10-联动期间各视图的独立操作)
- [11. 与其他功能的关系](#11-与其他功能的关系)
- [12. 光标清除时机一览](#12-光标清除时机一览)
- [13. 常见问题](#13-常见问题)
- [附：技术实现索引](#附技术实现索引)

---

## 1. 功能简介

光标联动开启后，在联动多图中的**任意一个视图**里点出或拖动十字光标，其余视图会**自动定位到同一个交易日**，实现多周期、多标的同屏对照。按目标视图周期与来源周期的大小关系，分为三种画面：

- 其余视图周期**严格更大**（例如来源是周线、另一视图是月/季线）：进入「**历史时点复盘**」——十字光标对准对应 K 线、横线锚定收盘价；光标所在的那根大周期 K 线会**按来源周期数据实时合成**（仿佛它正在形成中），而它**之后直到最新的所有 K 线统一淡化**，该光标点的指标数值也实时按合成 K 线计算；复盘十字光标**只保留顶部日期标签与左侧收盘价标签**，不显示"至最新涨幅"类标签；该视图单指仍可正常平移 / 上下缩放窗口，并可在主图区域点出一个纯本地「第二个十字光标」（见 6.3）——不接管来源、不取消整组联动（同周期视图不支持第二光标）；
- 其余视图与来源视图**周期相同**（日→日、周→周，含同周期不同标的）：仅显示普通**十字光标**（竖线定位同日/最近 K 线、横线锚定收盘价），**不合成、其后的 K 线也不淡化**；
- 其余视图周期**更小**（例如来源是周线、另一视图是日线）：不显示联动十字光标，改为用**两根蓝色竖轴 + 淡蓝底纹**框出来源那根大周期 K 线所覆盖的整段时间（周一～周五的全部日 K、整月的全部日 K/周 K……），并自动滚动、必要时自动缩小缩放级别让整段范围入屏；这种视图里还可以由用户**自行点出一个纯本地的「第二个十字光标」**查看本视图任意 K 线读数（见 6.3），它不影响联动。

同步只认**日期**（YYYYMMDD），不认价格：各视图的合成与数值永远基于**本视图自己标的**的数据。

> **谁是「来源视图」**：本次联动会话中**亲手点出（或拖出）十字光标的那个视图**即为唯一来源，会话期间来源固定、不会因在其他视图操作而被"接管"；整组联动光标只有在来源视图再点一下时才会取消（见第 8 节）。

## 2. 前置概念：联动多图模式

- 单图状态下点击右上角「**联**」即进入联动多图模式：屏幕横向排布 **2 / 3 / 4 个视图**，每个视图可独立设置**标的**与**周期**（日线 / 周线 / 月线 / 季线 / 年线）。
- 首次对某标的使用时，默认是 **2 个视图：左日线、右周线**（同一标的）。视图数量在「⚙ K线设置 → 联动视图数量」中修改，配置按主标的分别记忆。
- 每个视图下方信息栏从左到右为：主图指标按钮、标的名称、标的代码、单视图重置按钮、最右侧的「钻取单图」按钮（›）。

> 注意：**进入联动多图 ≠ 光标已经联动**。刚进入时「联」字是灰色，各视图光标相互独立；需要再点一次「联」开启后才会同步（见下节）。

## 3. 开启与关闭光标联动

「联」字按钮在不同状态下的含义：

| 所处状态 | 「联」字颜色 | 点击行为 |
| --- | --- | --- |
| 普通单图 | 灰色 | 进入联动多图模式（光标联动默认**关闭**） |
| 联动多图 · 联动关闭 | 灰色 | **开启**光标联动（所有视图按日期同步） |
| 联动多图 · 联动开启 | **蓝色** | **关闭**光标联动，各视图恢复独立 |

说明：

- 开启或关闭的瞬间，屏幕上**所有视图的十字光标 / 范围框 / 复盘淡化效果 / 各视图本地第二个十字光标会被一次性清空**，需要重新在任一视图点出。这样可以保证开关切换时没有残留状态。
- 关闭联动后，各视图十字光标完全独立，互不影响；视图的标的、周期、缩放位置均不改变。
- 退出联动多图（点左上角返回）或从信息栏「›」钻取单图时，联动光标与第二光标同样会清空；从钻取单图返回联动页时，之前的联动开关状态会自动恢复（但光标需重新点出）。

## 4. 基本操作

- **点出联动光标（=确定来源视图）**：在任一视图的主图 / 副图一 / 副图二区域轻点，即在该位置放出十字光标，**该视图即成为本次联动的唯一来源视图**，其余视图立刻按三种画面跟随。会话期间来源固定，在其他视图里点击或拖动**不会接管来源**。
- **拖动来源光标**：在来源视图手指按住后拖动，光标逐根 K 线移动，其余视图实时跟随（拖动超过轻微阈值才判定为拖动，避免误触）。
- **取消整组联动**：只有在**来源视图**已有光标的情况下再点一下，全部视图的联动光标 / 范围框 / 复盘淡化效果，以及各视图已放置的**第二个十字光标**才会一起消失。
- **非来源视图不能取消整组联动**，按其画面形态分别响应（详见第 8 节）：
  - 同周期画面：忽略一切单指点击与拖动（双指照常）；
  - 大周期复盘画面：单指拖动 = 正常平移 / 上下缩放窗口；在**主图区域**轻点可放置 / 取消**本视图自己的第二个十字光标**（副图一 / 副图二 / 副图三区域轻点不放置），第二光标纯本地，不接管来源、不取消整组联动；
  - 小周期范围框画面：在**主图区域**轻点放置 / 取消**本视图自己的第二个十字光标**（副图一 / 副图二 / 副图三区域轻点不放置），拖动行为见 6.3。
- 联动多图下不提供单图模式的 📌 固定光标（双光标对比）功能；联动十字光标每视图至多一个，大周期复盘视图 / 小周期范围框视图除此之外还可有一个纯本地第二光标（同周期视图没有，见 6.3）。
- **副图三（最下方副图）面板不响应任何触摸手势**（单图模式同样如此）：在它区域内轻点 / 拖动不会放置或移动十字光标与第二个十字光标，也不响应双指缩放与左右滑动——该区域预留给后续的虚拟按钮功能。十字光标的竖线仍会贯穿副图三、指标曲线与数值照常显示，副图三上方的指标名称 / 参数按钮也可正常点击；如需放置或拖动联动十字光标，请在主图、副图一或副图二区域操作（大周期复盘 / 小周期范围框视图的第二个十字光标仅在主图区域放置，见 6.3）。

## 5. 联动核心规则

1. **同步基准是交易日日期**：来源视图把当前光标所指 K 线的日期（以及来源周期）广播给所有视图，各视图按日期在自己的数据中定位。
2. **只同步时间，不同步价格；各算各的标的**：被联动视图的开、收、高、低、涨跌幅、成交量额及各副图指标，全部基于**本视图自己标的**的数据计算——即使视图之间是不同标的，也互不借用数据。
3. **周期大小决定画面形态**：
   - 目标视图周期 **严格大于**来源周期 → 「历史时点复盘」（合成 K 线 + 未来淡化 + 指标跟随，见 6.1）；
   - 目标视图周期 **= 来源周期**（含跨标的）→ 普通十字光标，不合成、不淡化；
   - 目标视图周期 **< 来源周期** → 「双竖轴范围框」（见 6.2）。
4. **大周期按"所属周期"定位，同周期按"最近交易日"定位**：
   - 目标周期**严格大于**来源（如周→月、周→季）：大周期 K 线只标注周期起始日，光标落在哪个周期区间，竖线就定位到**包含该日期的那根 K 线**（6 月 3 日在 Q2 内，就定位到 Q2，即使它距离 Q3 起始日更近）——这保证同一季度内移动周线光标时，季线光标稳定停在同一根；
   - **同周期**（含跨标的）：该日没有 K 线（停牌、不同标的）时，定位到时间上**最接近的一根** K 线。
5. **来源视图自身不动**：您手指操作的那个视图窗口位置保持不变，只有被联动视图会自动滚动定位。
6. **来源在会话内唯一且固定**：在哪个视图点出光标，哪个视图就是本次联动的来源；之后在其他视图的操作不会接管来源（同周期视图忽略一切单指操作；大周期复盘 / 小周期范围框视图只能平移缩放窗口、放纯本地第二光标）。想换一个视图当来源：先在来源视图再点一下取消整组联动，再到目标视图点出。
7. **大周期复盘 / 小周期范围框视图可有一个纯本地第二光标**：它不参与广播、不改变任何联动状态（含复盘合成与淡化），也不能取消整组联动，仅供查看本视图读数；同周期视图没有第二光标（详见 6.3）。

### 周期对照表

下表为"在某周期视图（行）触发光标后，其它周期视图（列）的反应"：

| 来源 ＼ 目标 | 日线 | 周线 | 月线 | 季线 | 年线 |
| --- | --- | --- | --- | --- | --- |
| **日线** | 十字光标 | 复盘·合成 | 复盘·合成 | 复盘·合成 | 复盘·合成 |
| **周线** | **范围框**（周一～周日） | 十字光标 | 复盘·合成 | 复盘·合成 | 复盘·合成 |
| **月线** | **范围框**（整月） | **范围框**（整月） | 十字光标 | 复盘·合成 | 复盘·合成 |
| **季线** | **范围框**（整季） | **范围框**（整季） | **范围框**（整季） | 十字光标 | 复盘·合成 |
| **年线** | **范围框**（整年） | **范围框**（整年） | **范围框**（整年） | **范围框**（整年） | 十字光标 |

> 「复盘·合成」= 光标所在大周期 K 线按来源周期实时合成、其后 K 线淡化；「十字光标」（同周期）= 光标那根就是真实 K 线，不合成、其后 K 线也不淡化。以上规律与视图标的是否相同无关：不同标的的视图用**自己的**来源周期数据完成合成。

## 6. 三种联动画面：历史时点复盘、十字光标 与 双竖轴范围框

### 6.1 历史时点复盘（目标周期严格大于来源周期）

被联动视图回答的问题是："**假如时间就停在来源光标这一天，本周期这张图当时是什么样子？**"画面由四部分组成。

#### 6.1.1 十字光标定位

- **竖线**：对准**包含光标日期**的那根本周期 K 线（同周期跨标的无当日 K 线时取最近交易日），贯穿主图与各副图，**顶部有日期（含星期）标签**；
- **横线**：锚定该时点 K 线的**收盘价**（合成情形下即最后一根被聚合来源 K 线的收盘价），贯穿主图，**左侧价格标签**显示该收盘价（开启多/空镜像时随镜像价格域取负）；
- 复盘态只保留上述两个标签**：不显示普通十字光标那样的横线右侧「光标收盘 → 屏幕末根收盘」标签，也不显示竖线底部「光标收盘 → 数据库最后一根」的距今涨幅标签——屏幕末根与数据库末根都属于光标之后的未来淡化区，"至未来"的读数在复盘语义下不成立（来源视图自身的光标仍显示全部标签）；
- 视图自动水平滚动，让该 K 线位于屏幕中央（见第 7 节）。
- **本视图仍可自由单指操作**：复盘合成 / 淡化画面保持跟随来源的同时，单指拖动照常平移窗口 / 上下缩放；在主图区域轻点还可放一个纯本地「第二个十字光标」查看本视图任意 K 线（见 6.3），蓝色第二光标与黑色联动复盘光标并存。第二光标不广播、不接管来源、不改变合成与淡化、再点也不会取消整组联动；本视图的指标数值栏与底部行情行始终跟随联动复盘光标，第二光标只带自身顶部日期与左侧价格两个标签。

#### 6.1.2 光标所在 K 线：正在形成中的「合成 K 线」（目标周期严格大于来源周期时）

- 该视图把**本标的**在当前大周期区间内、从周期起点到光标日期之间的**所有来源周期 K 线**聚合成一根"临时 K 线"：
  - **开** = 区间内第一根来源 K 线的开盘价；
  - **收** = 光标所指那根来源 K 线的收盘价（也是横线锚定价）；
  - **高 / 低** = 区间内所有来源 K 线最高价的最大者 / 最低价的最小者；
  - **成交量 / 成交额** = 区间内来源 K 线累加；
  - 红绿涨跌按"合成开 vs 合成收"实时翻转。
- 聚合的起点是**目标大周期自己的起始边界**（月线=该月首个交易日、季线=该季首个交易日，含光标日期之前但属于同一大周期的所有来源 K 线），而不是从光标当天才开始。
- 该合成 K 线**颜色正常、不淡化**，并随来源光标移动**实时变形**：光标在同一根大周期 K 线内移动时，每跨过一根来源 K 线，合成结果就纳入这根新 K 线（开不变、收/高/低/量实时更新）；光标跨入下一根大周期 K 线后，竖线跳到新的一根，合成重新开始。
- 跨标的时同样适用：视图显示标的 B 的月线、来源是标的 A 的周线光标时，B 的月线由 **B 自己的周线**合成（各视图互不借数据）。
- **同周期（日→日、周→周等，含跨标的）不进入复盘**：光标对准的本来就是一根真实存在的 K 线，只显示普通十字光标，不合成、其后的 K 线也不淡化。
- **特殊情形**：某视图在聚合区间内一根来源 K 线都没有（该标的整段停牌 / 无行情）时，光标那根回退显示真实库中最近 K 线，不做合成；其后的淡化仍然生效。

#### 6.1.3 光标之后：「未来区域」统一淡化

- 从合成 / 光标 K 线的**下一根开始，直到数据库最后一根 K 线**，统一以约 **1/3 透明度**淡化显示，表达"站在光标时点，这些 K 线尚未发生"；
- 淡化覆盖该视图的**全部图形元素**：主图蜡烛（实体与影线，空心 / 实心 / 收盘线 / OHLC 四种样式一致）、成交量 / 成交额柱、主图与全部副图的指标曲线，以及最新价虚线等参考线；
- 淡化**只改透明度，不改任何数值**；光标所在 K 线本身始终正常颜色。
- 光标落在数据库最新一根 K 线上时，后面没有 K 线，淡化区自然为空（合成结果也与库中最新未完结 K 线一致，画面等同于无复盘）。

#### 6.1.4 光标点指标实时跟随

- 光标所在 K 线的**主图指标、各副图指标、成交量额**均按 6.1.2 的合成 K 线实时重算：主图曲线在该点的位置、各副图数值栏读数都是"截至光标日期"的值；
- 该点**之前**的历史指标数值与曲线段保持原样、不变；
- 该点**之后**（淡化区）的指标数值与曲线**始终保持数据库原样**，不随光标变化（它们属于"尚未发生"的部分）；
- 快速拖动来源光标时，K 线合成图形始终实时跟手；指标重算在后台进行，极端快速滑动时该点数值可能有**极短暂的过渡延迟**（先显示上一次结果，算出后立即刷新），停顿或回看时均为精确值。

#### 6.1.5 光标消失即全部复原

- 来源光标以任何方式消失后（再点取消、关闭联动、开「边」、钻取、退出等），该视图的合成 K 线、淡化效果、指标重算结果全部撤销，K 线图形、颜色、指标数值与曲线恢复到光标出现前的数据库真实形态，不残留任何临时状态。

### 6.2 双竖轴范围框（目标周期 < 来源周期）

当来源是更大周期的一根 K 线时，它在小周期视图里对应多根 K 线，无法用单一光标表达，因此：

- 画面上出现**两根半透明蓝色竖轴**（无文字标签），两轴之间有**淡蓝色底纹**；
- 左轴 = 来源 K 线覆盖区间的**起始日**，右轴 = **结束日**（周线固定周一～周日，月/季/年线为整月/整季/整年，均含首尾）；
- 此状态下该视图**不显示联动十字光标**，仅表达"来源这根大 K 线对应本视图的这一段"；但用户可以自行点出本地第二个十字光标查看具体 K 线读数（见 6.3）。

范围框的绘制与拖动细节：

- **纵向按面板分段**：竖轴与底纹只穿过主图、副图一、副图二、副图三四个绘图面板，与十字光标竖线一样被三个副图指标栏自然断开；**不覆盖副图指标栏，也不穿过底部时间轴与行情数据栏**（在时间轴顶端截停）；
- **部分移出窗口不消失**：手动平移把任意一根竖轴拖出视图后，屏内的另一根轴与底纹仍正常显示，底纹在屏幕边缘截断（不会画到相邻视图）；两根轴都移出窗口后才整体不可见，平移回来自动恢复；
- **与 K 线同速跟手**：拖动平移时范围框随手指做亚像素级移动，与蜡烛图完全同步，不会一根一根跳（慢半拍）。

范围框不出现的情况（属于正常现象）：

- 该视图在覆盖区间内**一根 K 线都没有**（长期停牌、标的不同且无重叠行情）；
- 区间右端**超出本视图已有数据**（例如最新一根周 K 所在周尚未结束、本地日线数据未更新到周末）——为避免框出不存在的数据，此时不绘制范围框。

### 6.3 复盘 / 范围框视图的本地「第二个十字光标」

大周期复盘视图想查看合成光标之外**某一根具体 K 线**的读数、小周期范围框视图想查看框内某根 K 线的读数时，都可以在该视图自行点出第二个十字光标。它与联动光标完全独立：

- **样式**：蓝色十字线，竖线按面板分段贯穿主图与三个副图；**只有顶部日期标签与横线左侧数值标签**（没有右侧对比、底部距今日标签）。复盘视图中它与黑色联动复盘十字光标**并存**（一蓝一黑两条竖线），范围框视图中它是屏内唯一十字线；
- **放置**：第二光标不存在时，在**主图区域**轻点即在落点放出（副图一、副图二、副图三区域不放）；同周期画面不提供第二光标；
- **本地取消**：第二光标已存在时，在该视图主图 / 副图一 / 副图二区域再轻点一下即**只取消本视图的第二光标**——来源视图的联动、其他视图的范围框 / 复盘画面 / 第二光标都不受影响，**绝不会取消整组联动**；
- **拖动**：第二光标存在时单指按住拖动 = 逐根移动第二光标。范围框视图中本视图主图 / 副图指标数值栏与底部行情行（开、收、高、低、涨跌幅、成交量额等）实时跟随它读数；复盘视图中这些读数始终跟随联动复盘光标（合成 as-of 值），第二光标仅靠自身顶部日期 / 左侧价格标签读数；
- **不影响联动**：第二光标纯本地，不广播日期、不改变来源视图的合成 K 线 / 淡化 / 指标、不会让其他视图出现第二份光标；各视图的第二光标相互独立，3 / 4 视图时可同时存在多个、互不干扰；
- **与平移缩放的关系**：第二光标**不存在**时，该视图单指左右平移、上下缩放、双指缩放全部照常；第二光标**存在**时单指拖动用于移动光标（不平移窗口），双指缩放始终可用（双指落下时第二光标取消并进入缩放，与全 App 双指即清光标的行为一致）；
- **随联动状态自动清理**：来源在其视图内移动光标时第二光标保留；但来源取消整组联动、开关「联」、开「边」、钻取、退出联动、本视图滑动切换周期 / 标的、或复盘 / 范围框形态因周期关系变化而消失（如变为同周期画面）时，第二光标会一并清除。

## 7. 自动居中与自动缩放

- **复盘型（十字光标）**：每次联动到达（含持续拖动来源光标），被联动视图都会自动水平滚动，把目标 K 线放到屏幕**水平中央**；目标 K 线本就在窗口内时也会重新居中，以保证多图视线对齐。
- **范围框型**：自动滚动让**整段覆盖范围居中可见**；
  - 当前窗口能装下整个范围 → 保持当前缩放，只平移；
  - 装不下 → **自动放大可见 K 线数**（即缩小 K 线、展示更长区间），最大可放大到该视图全部 K 线，不会无限缩放。
- **双指缩放优先**：自动定位若发生在您正用双指缩放某视图时，会让位于您的手指、不强行打断；手势结束后后续联动照常居中。
- 自动滚动 / 缩放只改变显示位置，**不会修改任何视图持久化的缩放级别**；您自己双指调整的缩放级别仍按（标的 + 周期）分别记忆。复盘的合成与淡化也**不改变价格坐标范围**（合成 K 线的高低永远在真实完整大周期 K 线范围之内），纵向画面不会缩放抖动。

## 8. 发起权与操作优先级

- **来源视图：联动会话的唯一发起方**：在哪个视图点出光标，该视图即成为来源，全部视图跟随它；会话期间来源身份固定。来源视图中：拖动 = 移动联动光标（实时广播），再点一下 = 取消整组联动（含所有视图的第二光标）。
- **更换来源的方式**：先在当前来源视图再点一下取消整组联动，再到目标视图点出——不能在联动进行中直接"接管"。
- **非来源的同周期视图：冻结单指操作**：轻点、单指拖动一律忽略（不接管来源、不平移窗口、不放光标），联动十字光标保持稳定；双指平移 / 缩放在独立手势层，不受影响。
- **非来源的大周期复盘视图：可平移缩放 + 本地第二光标**：复盘合成 / 淡化保持跟随来源的同时，单指拖动恢复为正常平移 / 上下缩放窗口；第二光标的放置（仅主图区域轻点）、取消与拖动规则与下方小周期范围框视图完全一致；第二光标不改变合成与淡化、不接管来源，本视图再点也**不会取消整组联动**（只有来源视图能取消）。
- **非来源的小周期范围框视图：本地第二光标 + 手势分流**：
  - 第二光标不存在：单指拖动 = 正常平移 / 上下缩放；**主图区域**轻点 = 放置第二光标（副图一 / 副图二 / 副图三区域轻点不放置）；
  - 第二光标已存在：单指拖动 = 移动第二光标；轻点 = 本地取消第二光标；
  - 任何时候双指缩放都可用（双指落下时第二光标取消）。
- **本视图手指操作优先**：来源视图正在拖动光标时暂时忽略外来联动，避免光标被外部同步拽走、手指下一帧又拉回造成闪烁；手指松开后自动恢复接收。
- **回声自动过滤**：视图只会响应"别人"发起的联动，自己广播出去的日期不会再反过来驱动自己，不会出现来回弹跳。

## 9. 典型场景示例

**场景一：默认日线 + 周线双视图（两种画面各看一次）**

1. 进入个股详情 → 点「联」进入双视图（左日线、右周线）→ 再点「联」变蓝，开启联动；
2. 在**左侧日线**视图拖动十字光标：右侧周线视图进入复盘——光标所在周 K 按本周已走过的日 K 实时合成（周中看到的就是"截至今天的本周 K 线"），其后的周 K 全部淡化；
3. 反过来在**右侧周线**视图拖动某根历史周 K：左侧日线视图出现**两根蓝轴**，框出该周一～周五的全部日 K，窗口自动调整到整周入屏。

**场景二：周线光标看月线 / 季线的"历史当时"（复盘核心场景）**

设置视图包含周线、月线、季线（同一标的；数据库最新日期 2026-09-10）。在周线视图把光标停在 **2025-10-09** 那根周 K：

- **月线视图**：竖线落在 2025 年 10 月这根月 K 上，该月 K 当前就等于 2025-10-09 这一根周 K；2025-11-03 那根月 K 起直到最新月 K 全部淡化；
- 周线光标向右移到 **2025-10-13**：月线 10 月这根实时变为「10-09 周 K + 10-13 周 K」的合成（开=10-09 的开，收=10-13 的收，高/低取两根包络，量累加），红绿随合成结果翻转；光标继续停留在 10 月内时，开盘价始终不变；
- **季线视图**：竖线落在 2025 Q4 季 K 上，合成从 **Q4 自己的起始边界**算起——即使 10-09 是国庆假期后首根交易日、不是季内第一根图形，合成也会把 Q4 起点至光标日间的所有周 K 全部纳入；2026 Q1 起的季 K 全部淡化；
- 月线、季线光标点的 MA / MACD / KDJ 等指标数值与曲线点同步显示"截至该周"的计算结果，淡化区指标保持原样；
- 光标移到 2025-11-03 那根周 K 时，月线竖线跳到 11 月这根、合成重新开始；10 月这根恢复为数据库中的真实完整月 K（且因已在光标之前，正常颜色显示）。

**场景三：同周期多标的对照**

把两个视图都设为日线、分别选择两只股票。在任一视图拖动光标：两个视图只显示普通十字光标，都定位到同一交易日（一方当日停牌则取最近交易日），不合成、K 线也不淡化，纯粹做同一时间点的横向对照。

**场景四：跨标的、跨周期复盘**

视图 A 显示甲股票周线、视图 B 显示乙股票月线。在 A 的周线视图拖动光标：B 的月线用**乙股票自己的周线数据**合成光标所在月 K，乙股票之后的月 K 淡化；甲的走势只提供"时间锚点"，数值上两视图互不影响。

**场景五：来源固定 + 小周期视图的第二个十字光标（3 视图）**

设置三个视图：左日线、中周线、右月线。在**右月线**视图点出光标（右月线 = 来源）：

- 左日线、中周线都出现范围框（分别框出整月的日 K / 周 K）；
- 在**左日线**主图区域的范围框内轻点：出现蓝色第二光标，底部行情行与指标栏显示该日 K 的读数；再点一下，左日线的第二光标本地消失——右月线的来源光标、中周线的范围框全程不变（副图区域轻点不放置第二光标）；
- 也可以在左日线、中周线**各放一个**第二光标，两者独立、互不影响；
- 此时在左日线单指拖动：第二光标逐根移动（不平移窗口）；若先把第二光标取消，单指拖动又恢复为正常平移 / 缩放；
- 在**中周线**（同为非来源的更小周期）或其他大周期视图上的操作不会"抢走"来源身份；想让周线当来源：先在右月线再点一下清除整组，再到中周线点出。

## 10. 联动期间各视图的独立操作

光标联动只同步"看哪一天"，以下操作每个视图仍各自独立、互不影响：

- **切周期**：在上方副图左右滑动（左滑更小周期、右滑更大周期）；
- **切标的**：在下方副图左右滑动切换候选列表中的上 / 下一个标的，或点副图二指标栏右侧 **🔍** 搜索任意标的；
- **副图三无手势**：不参与上述任何滑动，也不响应缩放 / 光标点按（预留虚拟按钮区，见第 4 节）；
- **双指缩放 / 单指平移**：主图、副图一、副图二区域可用；缩放级别按（标的 + 周期）分别持久记忆。联动会话中的手势限制见第 8 节：非来源的同周期视图单指操作被冻结（双指照常）；非来源的大周期复盘 / 小周期范围框视图单指可正常平移缩放，第二光标存在时单指拖动用于移动第二光标；
- **主副图指标、指标参数、公式编辑**：各视图独立配置。

切换某视图的周期或标的后，该视图**仍是联动成员**，联动开关状态不变；若此时正有联动光标，新视图会按同一日期立刻重新进入对应画面（复盘或范围框）。

## 11. 与其他功能的关系

- **「边」分界线调节**：联动多图工具栏用「边」按钮替代了单图的 📌。开启后可拖动视图间分界线调整宽度比例（2 视图可调宽；3 / 4 视图等宽排布）；调节期间**禁止新建 / 拖动十字光标**，以免手势冲突。**开启「边」的瞬间会清空当前所有视图的十字光标 / 范围框 / 复盘效果（一次清场）**，关闭「边」后光标不会自动恢复，需要时重新在任一视图点出；联动开关本身保持开启。
- **📌 固定光标（双光标对比）**：仅单图模式提供；联动多图下不显示该按钮，每个视图最多一个光标。
- **钻取单图（信息栏最右 ›）**：把某一格临时放大为单图查看。进入钻取会清空联动光标与复盘效果，钻取期间不参与联动；点返回回到联动页，原联动开关 / 边线等状态自动恢复。
- **退出联动**：左上角返回先退出联动多图（自动关闭联动、清空光标与复盘效果），再点一次返回才关闭详情页。
- **重置视图配置**：信息栏每格的重置按钮（改过标的 / 周期后高亮）只恢复该格的默认标的与周期，不影响联动开关。

## 12. 光标清除时机一览

| 时机 | 联动画面（光标 / 范围框 / 合成与淡化） | 各视图第二个十字光标 | 联动开关状态 |
| --- | --- | --- | --- |
| 在来源视图再点一下取消光标 | 整组清除，各视图恢复真实数据形态 | **全部清除** | 保持开启 |
| 在复盘 / 小周期非来源视图再点一下 | 不受影响（来源与其他视图保持联动） | **仅本视图的清除** | 保持开启 |
| 复盘 / 小周期非来源视图双指落下缩放 | 不受影响 | 本视图的清除 | 保持开启 |
| 复盘 / 小周期非来源视图滑动切周期 / 切标的 | 新周期 / 标的重新进入对应画面 | 本视图的清除 | 保持开启 |
| 来源周期 / 标的变化使复盘 / 范围框形态消失（如变为同周期画面） | 按新关系重算画面 | 对应视图的清除 | 保持开启 |
| 点「联」开启联动 | 先清空，再重新使用 | 全部清除 | 变为开启（蓝） |
| 点「联」关闭联动 | 全部清除 | 全部清除 | 变为关闭（灰） |
| 开启「边」边线调节 | **清除**（本地光标 + 联动光标 / 范围框 / 复盘效果一并清场） | 全部清除 | 保持开启；关「边」后需重新点出 |
| 信息栏「›」钻取单图 | 全部清除 | 全部清除 | 临时挂起，返回时恢复 |
| 左上角返回退出联动 | 全部清除 | 全部清除 | 随联动一起退出 |

## 13. 常见问题

**Q：什么是「历史时点复盘」？为什么光标后面的 K 线变淡了？**
A：这是联动开启后、目标视图周期**严格大于**来源周期时的表达方式（如周线光标看月/季线）：十字光标声明一个历史时间点，光标所在的大周期 K 线显示为"截至该天正在形成"的合成 K 线，而它之后的 K 线属于"当时尚未发生的未来"，统一淡化到约 1/3 透明度（包括蜡烛、量柱和指标曲线）。这样您一眼就能区分"当时已经走出的行情"和"后来才发生的行情"。数值没有被修改，淡化只是视觉表达。同周期视图（日→日等）不进入复盘，没有淡化效果。

**Q：光标那根大周期 K 线为什么会自己动、数字一直在变？**
A：那是按来源周期数据实时合成的临时 K 线：来源光标在同一个月 / 季内每跨过一根 K 线，合成就多纳入一根（开取首根、收取最新、高低取包络、量额累加）。等光标跨入下一个月 / 季，竖线跳到新 K 线、合成重新开始。它不是数据库里的数据，光标消失后该位置恢复为真实完整 K 线。

**Q：合成时为什么从月 / 季的开头算起，而不是从我点的那天算起？**
A：因为一根月 K 的"形成中"状态天然包含本月此前已经走完的所有来源 K 线。比如 11 月中旬看月线，11 月这根应当包含月初到当天的全部周 K / 日 K，而不只是光标当天那一根。季线、年线同理，从该季 / 该年自己的第一个交易日开始聚合。

**Q：两个视图是不同股票，合成会不会串数据？**
A：不会。同步的只有时间（日期），每个视图只用**自己标的**的小周期数据合成自己的大周期 K 线，标的之间互不借用。

**Q：为什么我在周线视图点了光标，日线视图出现的是两根蓝线而不是十字光标？**
A：这是专门的"范围框"表达——一根周 K 对应周一到周五的多根日 K，单根光标无法表达这种一对多关系。蓝轴 + 淡蓝底纹框出的就是这根周 K 覆盖的全部日 K。

**Q：为什么有时候小周期视图连蓝轴也不显示？**
A：该视图在来源 K 线覆盖的时间段内没有任何 K 线（停牌、标的无重叠行情），或覆盖区间右端超出了该视图已有的数据（如本周尚未结束、数据未更新）。更新数据或选择已完结的历史 K 线即可正常显示。

**Q：被联动视图里十字光标的横线对准哪个价格？**
A：复盘情形下横线对准合成 K 线的收盘价（即光标所指来源 K 线的收盘价）；同周期情形下就是该真实 K 线的收盘价。左侧标签、底部行情行的"收"三者一致。您手指直接操作的来源视图里，横线仍跟随手指位置，不受影响。

**Q：拖动来源光标时，大周期视图的指标数字偶尔慢半拍？**
A：指标重算在后台进行以保证拖动不卡顿，K 线合成图形始终是实时的；极端快速滑动时指标数值可能短暂显示上一次结果，停住或回看（命中缓存）时即为精确值。

**Q：拖动时各个窗口为什么会自己滑动？**
A：自动居中机制，用于让所有视图的同一时间点处于相近视线位置。您正在直接用手指操作的那个窗口不会被移动；双指缩放某视图时自动定位也会临时让路。

**Q：在一个视图上拖动，会被其它视图的联动"抢"光标吗？**
A：不会。来源视图拖动期间忽略外来联动；松手后才重新接收。非来源视图也不能"接管"来源——同周期非来源视图忽略一切单指操作；大周期复盘 / 小周期范围框非来源视图的单指只用于平移缩放和纯本地第二光标，不对外广播；想换一个视图当来源，请先在来源视图再点一下取消整组联动，再到目标视图点出。

**Q：复盘 / 范围框视图里点出来的蓝色十字光标是什么？会影响联动吗？**
A：那是该视图**纯本地的第二个十字光标**：范围框视图用它查看框内某根具体 K 线，复盘视图用它在不打断联动的情况下查看任意 K 线（复盘视图中它与黑色联动复盘光标并存，合成 / 淡化照常不变）。它不广播、不同步：来源视图的状态不变，其他视图也不会出现第二份；多个视图可各放一个、互不影响。再点一下只取消本视图的第二光标、绝不会取消整组联动；只有来源视图取消整组联动时才会清掉全部第二光标。同周期画面不提供第二光标。

**Q：为什么在同周期的非来源视图里点击、单指拖动没反应？**
A：这是有意的会话保护：非来源的同周期视图不允许接管或误清整组联动，联动十字光标保持稳定，需要平移 / 缩放该视图时用双指即可。大周期复盘视图**不受此限**：单指可正常平移 / 上下缩放，也能在主图区域放本地第二光标（但同样不接管来源、不取消整组联动）；小周期范围框视图则可用第二个十字光标查看具体 K 线读数。

**Q：复盘 / 范围框视图还没放出第二个光标时，单指拖动为什么能正常平移窗口？**
A：手势按第二光标是否存在分流：不存在时单指 / 双指都照常平移缩放（复盘视图还可上下缩放）；第二光标存在时单指拖动才用于移动它（再点即可取消，恢复平移）。

**Q：范围框为什么没穿过指标栏、时间轴和行情数据栏？**
A：范围框与十字光标竖线采用相同的纵向规则：只覆盖主图与三个副图的绘图面板，被指标栏自然断开，在时间轴顶端截停。

**Q：平移时一根蓝轴滑出屏幕，范围框怎么少了一根轴？**
A：属正常表现：范围框按可见窗口裁剪，哪根轴在屏内就画哪根，底纹贴屏幕边缘截断；两根轴都移出窗口时范围框整体不可见，平移回来自动恢复。范围框与 K 线同速跟手，亚像素移动不会跳格。

**Q：复盘大周期视图的十字光标为什么没有右侧和底部的涨幅标签？**
A：这两个标签分别是"光标收盘相对屏幕末根 / 数据库最后一根"的涨幅，而末根 K 线在复盘态属于光标之后的未来淡化区，"至未来"的读数在"假如时间停在光标当天"的语义下不成立，故复盘画面只保留顶部日期标签与左侧收盘价标签；来源视图自身的光标仍显示全部标签。

**Q：光标联动会同步各视图的缩放和窗口位置吗？**
A：不会。只同步日期；各视图缩放级别、平移位置独立并分别记忆。仅在需要让目标 K 线 / 范围入屏时，被联动视图会临时自动居中或放大可见区间。

---

## 附：技术实现索引

供开发维护参考，普通用户无需阅读：

- 联动同步对象（广播光标日期、来源周期、覆盖范围、**来源视图身份**）：[KlineDetailView.swift](Kline/KlineDetailView.swift) 中的 `DualLinkSync`（`cursorDate` / `sourcePeriod` / `sourceRange` / `sourceID`）
- 「联」按钮开关状态、清光标广播令牌、钻取快照：`KlineDetailView.swift` 中的 `cursorLinkEnabled` / `cursorClearToken` / `drillInSnapshot`
- 光标的发布与接收（防回声、防拖动冲突、居中、范围框定位、**非来源身份判定**）：[KlineChartView.swift](Kline/KlineChartView.swift) 中的 `publishLinkCursor` / `applyLinkCursor` / `renderCursorIndex` / `linkRangeIndices` / `centerLinkRange` / `isLinkedNonSource` / `isLinkedReplayView` / `isLinkedSecondCursorView` / `isLinkedFrozenView`
- 周期粒度等级与大周期 K 线覆盖区间计算：[KlineData.swift](Kline/KlineData.swift) 中的 `KlinePeriod.granularityRank` / `KlinePeriod.periodDateRange`
- 范围框渲染（双蓝轴 + 淡蓝底纹，**按面板分段、按可见窗口裁剪、跟随 panOffset 亚像素平移**）：`KlineChartView.swift` 中的 `linkRangeAxisOverlay`（装饰覆盖层整体 `allowsHitTesting(false)` + `clipped()`）
- 非来源复盘 / 范围框视图的本地「第二个十字光标」：`KlineChartView.swift` 中的 `isLinkedReplayView` / `secondCursorIndex` / `secondCursorY` / `clearSecondCursor` / `legendCursorIndex` / `secondCursorHorizontalOverlay`（竖线复用 `mainCursorVLine` / `subCursorVLine` 的 `secondary` 入参；`isLinkedFrozenView` 收敛为仅同周期非来源视图）
- 复盘十字光标标签精简（仅顶部日期 + 左侧收盘价）：`mainCursorVLine` 与横线右侧标签处的 `linkReplayState?.idx == index` 守卫
- 联动视图数量 / 标的 / 周期配置与缩放持久化：[LinkedViewStore.swift](Kline/LinkedViewStore.swift)（`Documents/LinkedViews.json`、`Documents/LinkedZooms.json`）
- 单格视图的数据加载与切周期 / 切标的手势：[LinkedKlineTile.swift](Kline/LinkedKlineTile.swift)
- 「历史时点复盘」核心实现（均在 [KlineChartView.swift](Kline/KlineChartView.swift)）：
  - 复盘派生与合成 K 线：`linkReplayState` / `synthesizeBar` / `cursorDisplayItem` / `linkedMetaID`
  - 来源周期数据共享懒加载：`LinkSourceBarCache`
  - 合成替换 / 未来淡化绘制：`SyntheticBar` / `SyntheticStick`，`MainChartCanvas` 与 `SubChartCanvas` 的 `syntheticBar` / `syntheticStick` / `dimFromIndex` 入参
  - 合成点指标异步重算：`AsOfTrigger` / `scheduleAsOf` / `evaluateAsOf` / `AsOfValueCache` / `asOfMain` / `asOfSubs`
- 「历史时点复盘」设计与实施记录：[linked-asof-replay-plan.md](.trae/documents/linked-asof-replay-plan.md)
