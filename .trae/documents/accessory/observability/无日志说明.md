# 本模块无日志（2026-09-19）

悬浮按钮**当前没有任何日志，且暂无需要**：它是纯本地 UI 状态（落位持久化 + 四态外观），没有网络、没有数据一致性、没有静默失败路径；而 `onChanged` 是高频回调，往里插日志只会刷屏。

若将来出现「点击无反应 / 落位不恢复」类问题需要运行时证据，**优先用 UITest 断言**（`accessory.button` 可点性、落位坐标是否与 `UserDefaults` 一致），而不是新增 print。

关联：[[悬浮按钮]]