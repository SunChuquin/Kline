# Checklist

## 交付物存在性与范围

- [x] `.trae/documents/python-engine/Python引擎下沉可行性分析.md` 已创建，且是本次唯一新增的文件（该目录下仅此 1 个文件；`.trae/specs/analyze-python-engine-offload/` 为 spec 工作流输入，不计入交付物）
- [x] 文档不含 spec 模式专用的 `## What Changes` / `## Impact` / `## ADDED Requirements` 三节
- [x] 未改动任何 Swift / Python 源码（`Kline/**`、`src/**`、`check_indicators.py` 均无改动）
- [x] 未改动 `Kline.xcodeproj/project.pbxproj`、`.github/workflows/**`、`Kline.entitlements`
- [x] 未引入、未下载 `Python.xcframework` 或任何二进制依赖
- [x] 未触发设备构建、安装或部署（无 `build_logs/`、无构建产物）

## 现状基线（事实层）

- [x] Swift 侧运行时计算职责表，每条含文件链接 + 行数 + 一句话算法说明
- [x] 覆盖：公式引擎、指标预计算与跳空缺口、回测引擎、条件单结算、模拟交易规则、东财取数、分片同步、增量库读写、主库合并、清单更新编排、本地 HTTP 服务、指标模板解析、策略 DSL、图表渲染与交互、SQLite 查询层、布局 schema
- [x] PC 侧 Python 模块表，每条含文件 + 行数 + 用途 + **第三方依赖列**
- [x] 明确写出「承载口径逻辑的五个数据模块全部为标准库实现，无 pandas / numpy / requests」，并注明全项目唯一第三方依赖是 GUI 壳 `src/tdx_gui.py`（**PySide2**，不承载口径）
- [x] 构建现状写明：Xcode `Check Indicators` Run Script 已在编译期调用 `python3 check_indicators.py`（跑在构建机上）
- [x] 构建现状写明：`PBXFileSystemSynchronizedRootGroup` + `Indicators/*.tdx` 的 `membershipExceptions`
- [x] 构建现状写明：CI 为 unsigned `xcodebuild` + 嵌入 `rootprobe`/`opener` + ad-hoc `codesign` 注入 entitlements + 打包 `.ipa` + 发布 `latest` Release
- [x] 构建现状列出 `Kline.entitlements` 的四个实际键值
- [x] **明确写出「CI 当前不存在任何 `TROLLSTORE` 条件打包分支」**
- [x] **明确写出「工程内无 embedded framework、无 SPM / CocoaPods 依赖」**
- [x] 基线章节不含推测性表述，全部可回溯到具体文件
- [x] 规模数字与仓库实测一致：Swift **149 个文件 / 48745 非空行**（非「1.6 万行 / 135 文件」）；`Kline/Chart` **18 文件 / 8515 行**（非「约 20 文件 / ≈4000」）；Python 合计 4421 行
- [x] 公式引擎内置函数数量与代码一致：**22 个具名内置函数**（另有 3 个小写别名、11 个序列键），非「约 40 个」

## 四维评估

- [x] 汇总表对每个候选都分列了「可读性 / 可维护性 / 版本更新灵活性 / 避免造轮子」四列，无合并（「通用性」单列在 §3.6.1，避免表格过宽）
- [x] 候选集合覆盖 15 项：指标模板解析与校验、TDX 公式引擎、周期聚合、东财取数口径、策略与公式 DSL 解析、回测引擎、条件单/预警判定层（7a）、条件单副作用层（7b）、预警记录（7c）、SQLite 查询层、本地 HTTP 自更新服务、图表渲染与手势、布局 schema 与编解码、**K 线形态识别（12，新）**、**通用数学 / 统计函数库（13，新）**
- [x] 结论分档且每档有成员：「推荐下沉」6 项（1/2/3/4/12/13）、「中性」1 项（5）、「中性偏不建议」1 项（7a）、「不建议下沉」7 项（6/7b/7c/8/9/10/11）
- [x] 候选 12 / 13 被显式标注为「新能力，仓库内暂无」，与「下沉已有功能」的性质差异有单独说明
- [x] 基线表包含 `SimTradingRules.swift`（158 行，T+1/整手/佣金/印花税/涨跌停/交易时段纯规则层）
- [x] 基线表在条件单行明确标注「『仅提醒』（预警）是同一引擎的 `isAlertOnly` 形态，不是独立引擎」
- [x] 每个推荐下沉的候选都写明了反向约束（调用频率 / 是否首帧 / 可否异步缓存）并说明「三关全过」
- [x] 至少有一个反例解释「看似合适却不应下沉」——候选 9（本地 HTTP 自更新服务）给出「更新器依赖被更新对象」的循环依赖理由
- [x] 「避免造轮子」维度写明了诚实结论：承载口径的 Python 全为标准库，兑现该维度收益需为 iOS 侧引入第三方库，并给出体积与交叉编译代价

## 两个争议候选的专项论证

- [x] TDX 公式引擎被明确上调为「中性 / 可部分下沉」，并给出上调理由（MyTT/pandas-ta/TA-Lib 覆盖 22 个手写具名内置函数；解析规则与 `check_indicators.py` 双实现）
- [x] 公式引擎的下调理由写清为两个具体工程约束：首帧 150 根近似路径、`TDXIncrementalState` 跨 FFI 状态机 + `TDXSharedSeries` 共享引用变拷贝
- [x] 公式引擎给出了具体切法（下沉解析 + 内置函数库，留原生首帧近似 + 分块调度）
- [x] 公式引擎给出了**阈值化条件**：Phase-0 实测若达标则可整体下沉，本分级可被推翻
- [x] 回测引擎的理由已从「性能」纠正为「语义一致性」，并引用三处代码证据（L36-43 复用 `StrategyCondGenerator.drafts`、L736-755 复用 `SimCondRule.gridLevelCount` 且 `batchQty` 为同口径重写、L339 止损腿同序）
- [x] 回测引擎写明了「与条件单结算成对」的结论，以及成对下沉需在行情刷新路径引入解释器开销
- [x] 明确否定了「回测是性能问题」这一不准确理由（整段数组进 / 净值曲线出的形状很干净）

## 条件单与预警专项论证

- [x] **明确纠正前提**：预警不是独立引擎，而是条件单的 `SimCondDirective.isAlertOnly` 形态，走同一条 `sweepConditions` 与同一个 `SimCondRule.evaluate`
- [x] 指出「按预警 / 条件单分别下沉」在代码里**不存在对应边界**
- [x] 给出真正存在的三层边界表（判定层 / 取数层 / 副作用层），并标明各自纯度
- [x] 引用 `SimCondRule` 的 `nonisolated` 声明注释作为「判定层已被三处消费者共享」的证据
- [x] 写明「判定层若下沉，则 §3.2 给回测引擎的语义一致性阻断点自动消失」这一推论
- [x] 写明「避免造轮子 = 0」：A 股 T+1 / 涨跌停 / 整手 / 佣金印花税 / 网格分批 / 追踪止损**无任何现成 Python 库**
- [x] 写明独有代价：判定层是**资金动作的授权闸门**，可远程替换的组件掌握授权，且副作用层留原生只能保证「写」安全、保证不了「判决」正确
- [x] 写明触发时机在行情刷新路径（含 11:00/14:30/15:05/17:30 四次定时），并说明按「整轮结算一次 FFI 调用」批量传递时开销可控、**不构成阻断项**
- [x] 结论为「只能整层搬、不能拆」+ 定级「中性偏不建议」+ 重新评估时机（出现自定义预警脚本需求，且需配签名与版本锁定）
- [x] §6.4 写明 7a 是全表唯一「形状很合适、但结论是别做」的候选
- [x] §6.3 有「明确不做」清单，且列出 7a 的重新评估时机

## 双路径打包方案评估

- [x] 给出了引擎体积量级估算，并与当前 IPA 体积（无 embedded framework、种子库 0.9MB）对比
- [x] 分析了运行时 `dlopen` 的可行性边界
- [x] 明确区分「已由 `Kline.entitlements` 与 CoreTrust 豁免推出的结论」（落盘可行）与「必须设备实测的假设」（dylib 签名校验 / arm64e 指针认证 / iOS 版本差异）
- [x] 指出「`CODE_SIGNING_ALLOWED=NO` + fakesign」本仓库 CI 已在做，不是新增能力
- [x] 分析了引擎未就绪时的门禁与降级，并给出「Python 只能承载非首帧、可异步、可缓存、缺失不影响核心可用性的功能」的硬结论
- [x] 分析了「更新器依赖被更新对象」的循环依赖风险，并给出三条规避方式
- [x] 逐条回应了用户原方案的五点（`TROLLSTORE=1` 条件化 / XcodeAnyTroll fakesign / iCloud 手动放置 / App 自行下载 dlopen / 「首次启动体验略有差异」），并指出需补强之处
- [x] 明确指出「剥离省下的体积与运行时再下载是同一笔账」，削弱了剥离的收益叙事

## 总评与结论

- [x] §6.1 给出明确可行性判断「有条件可行」，并说明条件是什么
- [x] §6.2 给出一句话可复述的推荐形态
- [x] §6.3 分阶段表每一阶段都有「进入条件」列且非空
- [x] §6.4 指出收益不对称：避免造轮子最被高估、可维护性最被低估且最确定
- [x] §7 Phase-0 实验共 3 条，每条都有通过判据
- [x] §7 实验 1 是 `dlopen` + `Py_Initialize`（附「失败则运行时获取路径作废」的判据）
- [x] §7 实验 2 覆盖引擎冷启动与 150 根指标计算耗时（作为候选 2 能否下沉的判据）
- [x] §7 实验 3 覆盖引擎缺失时的全功能可用性
- [x] 读者只读 §6 即可知道「做不做、先做什么、满足什么条件才继续」

## mermaid-doc-convention 自检

- [x] 全文档配图 = 2 张，未超（独立复核 ```mermaid 代码块实际数量 = 2）
- [x] 图 1（筛选判据）只讲一件事：职责筛选的决策路径
- [x] 图 1 节点链长 ≤5（3 层），节点 id 全 ASCII，中文只在 `{}` / `[]` 标签内
- [x] 图 1 前有「从 `[q1]` 起读」的导语
- [x] 图 1 后有「图里没画出来的部分」（3 条）
- [x] 图 2（双路径构建与引擎获取）只讲一件事：两条构建路径的产物与引擎来源差异
- [x] 图 2 两条链分别为 2 与 4 节点，无跨 subgraph 长箭头
- [x] 图 2 前有「从 `[xc]` 与 `[ts]` 两个 subgraph 分别起读」的导语
- [x] 图 2 后有「图里没画出来的部分」（3 条）
- [x] 图中未塞入本该放正文的细节（字段清单、SQL、长函数签名）
- [x] 能用文字列表表达的内容（基线表、评估表、阶段表、实验表）均未被画成图
- [x] 配色仅用于区分 subgraph 归属，未做装饰性堆色（全文无 `style` 指令）

## 事实性复核（独立校验代理 + 人工复算）

- [x] §1.1 全部 19 个 Swift 文件行数与实测一致
- [x] §1.2 全部 6 个 Python 文件行数与实测一致
- [x] §3.2 / §3.3 的 17 处代码引用逐条打开文件核对，位置与语义相符（其中 `SimConditionModels` 的 `isAlertOnly` 已从 L131 修正为 L151）
- [x] §1.3 七条构建现状断言全部与 `project.pbxproj` / `build.yml` / `Kline.entitlements` / 仓库文件实测一致
- [x] 修正后的 6 处事实（Swift 规模、Chart 规模、`tdx_gui.py` 依赖、内置函数数量、`batchQty` 复用表述、`isAlertOnly` 行号）已在 `spec.md` 与交付物中同步

## 本轮修订 v2（用户四点反馈）

### 反馈 1：数学引擎 / 「Swift 只是壳」

- [x] 新增 §2.1「补一道判据：通用性」，给出「通用计算层 / 平台外壳层」两分法与判据（换宿主后是否原样还有用）
- [x] §2.1 明写这条判据**不在** mermaid 图内（图的「图里没画出来的部分」已补第 4 条说明），避免读者以为图是完整筛法
- [x] 新增 §3.6.1 重排表，明确「通用性」是**唯一会改变分级**的维度
- [x] 候选 2 由「中性 / 可部分下沉」上调为「推荐下沉（解析层 + 函数库），首帧调度留原生」，并写明**上调不依赖任何未验证假设**（解析层本就不在首帧路径）
- [x] 明写候选 4 上「版本灵活性」与「通用性」**方向相反**，并解释为何结论不变（通用性损失本就存在，非下沉新增）
- [x] 新增 §3.6.2 候选 13「通用数学 / 统计函数库」，点明这是**唯一一条只有在 Python 侧才能获得的价值**（Swift 无 numpy/scipy 对等物）
- [x] §3.6.2 明写代价：numpy 数十 MB 与 §5.1「剥离缩小 IPA」**直接冲突**，二者不可兼得，且这是全文唯一无法用工程手段化解的取舍

### 反馈 2：K 线形态识别（不造轮子 + 怎么实现与验证）

- [x] 新增 §3.6.3 候选 12，先声明**仓库内零形态识别代码**（grep 无 CDL / 锤子 / 吞没 / 启明星），定性为「新能力该建在哪一侧」
- [x] 给出「为什么天然适合 Python」四条判据表（避免造轮子最强正 / 非首帧 / 可异步可缓存 / 批量无回调）
- [x] 给出**必须二选一**的路线判断：A. TA-Lib（C）交叉编译为 iOS framework，Swift 直调，**不需要引擎**；B. 纯 Python 形态实现，走引擎。并解释为何「Python 里的 C 扩展」在 iOS 上比单独 framework 更难
- [x] 给出实现形状（输入 / 输出 / 落点 / 增量友好）
- [x] 给出**三层验证方案**：① 合成序列单元测试 ② 参考实现差分测试（golden 数据集 / 哈希，与既有基线库比对哲学一致）③ 可视化抽查
- [x] 给出**动手前必须先定死的三件事**（参考实现选谁 / 参数阈值锁哪一套 / 先做哪几个形态），并说明「不锁定就必然两端不一致」
- [x] §7 加说明：候选 12 的验证**不走** Phase-0（它不是引擎可行性实验），并给出交叉引用

### 反馈 3：`src/` 的定位澄清

- [x] 新增 §1.4「`src/` 的定位」，写明它**不打算放进 App 内运行**，同仓目的是统一版本管理
- [x] §1.4 指出同仓目前只达成**源码级统一**，而引擎下沉可达成**运行时级统一**（同一份 `.py` 两端执行），并说明这比「消除双实现」更准确
- [x] §1.4 明写代价：`src/` 将从「纯 PC 工具」变为「两端共享库」，带来 GUI 壳物理隔离 / 不能假设 PC 路径 / 标准库版本兼容三条约束
- [x] §1.4 指出好消息：`tdx_parser.py` / `live_db_builder.py` / `txt_changes.py` 本就无第三方依赖，天然满足共享前提
- [x] §6.3 阶段 0 表述已调整：承认同仓管理**已在做**，本阶段补的是「靠注释对齐 → 靠测试锁死」，且**不需要引擎**

### 反馈 4：「信任代价」「两个副作用」展开 + 耦合核实

- [x] §3.3 新增「先把两个术语说清楚」小节
- [x] 「信任代价」展开为**四件事**：判决逻辑脱离编译产物 / 谁掌握引擎包谁掌握授权 / 错的代价不对称且不可逆 / 可审计性下降
- [x] 「两个副作用」用独立表格展开：取数层 = **读副作用**（`@MainActor`、触发预取，故非纯函数）；副作用层 = **写副作用**（唯一允许改账户状态处）
- [x] 明写「只有判定层是真纯函数」，并给出「能搬的只有中间那一层，而那一层恰好就是判决本身」的结论
- [x] §3.3 新增「核实：条件单 / 预警与指标引擎到底是不是同源」小节，结论为**语义上成立、代码依赖上不成立**
- [x] 给出三条代码证据：`SimCondSnapshot` 只有六个标量（L19-L26）/ 8 种 kind 全是标量比较 / `snapshot.ma` 来自 `MarketRow.swift L129` 的独立 `ma()` 而非 TDX 引擎
- [x] 给出「同构，不是同源」对照表（形状 / 输入 / 扩展方式 / 依赖 / 动作五个维度）
- [x] 纠正上一版不准确处：真正与指标引擎耦合的是**公式引擎的三个消费者**（图表指标 / 表头筛选公式列 `MarketRowCache.swift L292` / 策略 DSL），不是条件单
- [x] 说明 B/S/T 标记与条件单页面记录属于**结果可视化**，与判定层解耦
- [x] 诚实标注：仓库内**未找到**主图 B/S/T 成交标记的绘制实现，已向用户说明并请其指认位置

### v2 一致性

- [x] 交付物与 `spec.md` 正文逐字同源（`spec.md` 由交付物正文重建，仅保留 spec 模式专用三节）
- [x] 全文档 mermaid 配图仍为 **2 张**（未因新增章节而加图）
- [x] §6.1 / 6.2 / 6.3 / 6.4 已同步本轮结论（数学栈、形态识别、阶段 0~3、通用性维度）
- [x] §6.3 阶段表由 3 阶段扩为 4 阶段，每阶段仍有非空「进入条件」
