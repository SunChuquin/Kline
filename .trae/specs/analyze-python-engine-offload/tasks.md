# Tasks

> 分析全文已写入 `spec.md`。实施阶段只剩两件事：落盘文档 + 自检。**不写任何代码，不改任何构建配置。**

- [x] Task 1: 落盘分析文档：把 `spec.md` 正文写入 `.trae/documents/python-engine/Python引擎下沉可行性分析.md`。
  - [x] SubTask 1.1: 确认 `.trae/documents/python-engine/` 目录不存在则创建；确认该目录下不产生第二个文件。
  - [x] SubTask 1.2: 复制 `spec.md` 的分析正文（「一、现状基线」至「七、Phase-0 验证实验」及文首说明），**剔除 spec 模式专用的 `## What Changes` / `## Impact` / `## ADDED Requirements` 三节**（它们是流程元信息，不属于分析交付物）。
  - [x] SubTask 1.3: 确认文档内的相对引用（`Kline/`、`src/`、`.github/workflows/`）与仓库当前路径一致。

- [x] Task 2: 配图合规自检并修正：按 `mermaid-doc-convention` 逐条核对两张图。
  - [x] SubTask 2.1: 核对图 1「筛选判据」：一图一事、节点链长 ≤5、节点 id 全 ASCII、中文只在 `[]` 标签内、图前有导语、图后有「图里没画出来的部分」（3 条）。
  - [x] SubTask 2.2: 核对图 2「双路径构建与引擎获取」：两条链分别为 2 与 4 节点、跨 subgraph 无长箭头、图前有导语（含从哪个 subgraph 起读）、图后有「图里没画出来的部分」（3 条）。
  - [x] SubTask 2.3: 确认全文档配图 ≤2 张；确认正文中可用列表表达的内容没有被硬画成图。
  - 自检结论：**全部通过，无需修正**。独立复核 ```mermaid 代码块实际数量 = 2；两张图节点 id 全 ASCII、无跨 subgraph 长箭头、图前导语与图后「图里没画出来的部分」齐备；全文无 `style` 指令（无装饰性堆色）。

- [x] Task 3: 逐条过 `checklist.md` 并修正：核对事实性、四维评估完整性、双路径评估覆盖面与结论可执行性。
  - [x] SubTask 3.1: 事实性核对——基线表中每个文件的行数与仓库当前状态一致；「CI 无 TROLLSTORE 条件分支」「PC 侧 Python 无第三方依赖」「工程无 embedded framework」三条否定事实均成立。
  - [x] SubTask 3.2: 覆盖度核对——§3.2 专项论证是否同时回答了「公式引擎为何上调为中性」与「回测引擎为何维持不建议」，且后者理由已从性能改成语义一致性。
  - [x] SubTask 3.3: 条件单 / 预警专项核对——§3.3 是否先纠正了「预警是独立引擎」这一前提、是否给出判定层 / 取数层 / 副作用层三层边界、是否写明「避免造轮子 = 0」与「授权闸门」这一独有代价、结论是否为「只能整层搬」。
  - [x] SubTask 3.4: 结论核对——§6.1 是否给出明确判断（有条件可行）、§6.2 是否一句话可复述、§6.3 每阶段是否都有进入条件且附「明确不做」清单、§6.4 是否指出 7a 是全表唯一「形状合适但别做」的候选、§7 是否每条实验都有通过判据。
  - 核对结论：除下述 6 处事实错误外，其余 60+ 条 checkpoints 全部通过。

- [x] Task 4: 修正事实性错误并同步 `spec.md`：独立校验代理发现 6 处声明与仓库实测不符，逐条修正。
  - [x] SubTask 4.1: Swift 规模数字：`1.6 万行 / 135 文件` → **`4.9 万行 / 149 文件（不含空行，实测 48745 行）`**；`Impact` 节同步。
  - [x] SubTask 4.2: `Kline/Chart/*` 规模：`约 20 文件 / ≈4000 行` → **`18 文件 / 8515 行`**（实测）。
  - [x] SubTask 4.3: 「PC 侧 Python 全部为标准库实现」→ 修正为**「承载口径逻辑的五个数据模块全部为标准库实现」**，并显式写出唯一第三方依赖是 `src/tdx_gui.py`（**PySide2**）；连带修正 §3.5 与 §6.4 的同一表述，以及 §1.2 的 `tdx_gui.py` 依赖列与「见 §3.4」的错误交叉引用（应为 §3.5）。
  - [x] SubTask 4.4: 公式引擎内置函数数量：`约 40 个` → **`22 个具名内置函数`**（另 3 个小写别名、11 个序列键）；实测 `FormulaEngine.swift` 具名 case 分支确为 22 个，文档原列举的 22 个名称全部存在。
  - [x] SubTask 4.5: `SimConditionModels.swift` 的 `isAlertOnly` 位置：`L131` → **`L151`**（L131 是 `alertOnly` 字段本身）。
  - [x] SubTask 4.6: `StrategyBacktestEngine.swift` L736-755 的表述：`复用 gridLevelCount 与 batchQty` → **`复用 gridLevelCount（注释「不重复实现」）；batchQty 为同口径重写（注释「与 SimCondRule.batchQty 同口径」）`**。
  - [x] SubTask 4.7: 上述 6 处在 `spec.md` 与交付物中同步修正；`checklist.md` 中被证伪的两条断言（标准库范围、内置函数数量）已按实测重写，并新增「事实性复核」小节。

- [x] Task 5: 按用户四点反馈修订分析（v2）：补「通用性」判据、新增两个候选、展开两个术语、核实耦合、同步结论。
  - [x] SubTask 5.1: 新增 §2.1「补一道判据：通用性」——「通用计算层 / 平台外壳层」两分法；并在图后「图里没画出来的部分」补第 4 条，声明该判据不在图内。
  - [x] SubTask 5.2: §3.1 表新增候选 12 / 13（标注「新能力，仓库内暂无」），并加说明区分「下沉已有功能」与「新能力建在哪一侧」；候选 2 分级上调为「推荐下沉（解析层 + 函数库）」。
  - [x] SubTask 5.3: §3.2 补「公式引擎有三个消费者」证据链（含新发现的 `MarketRowCache.swift L292` 表头筛选公式列）；阈值化条件改为「仅影响首帧调度能否一并下沉，不影响上调结论」。
  - [x] SubTask 5.4: §3.3 新增「先把两个术语说清楚」：信任代价展开为四件事；两个副作用用表格区分读副作用（取数层）与写副作用（副作用层）。
  - [x] SubTask 5.5: §3.3 新增「核实：条件单 / 预警与指标引擎是不是同源」，给出三条代码证据 + 「同构不是同源」对照表 + 公式引擎三消费者纠正 + B/S/T 未找到的诚实标注。
  - [x] SubTask 5.6: 新增 §3.6（3.6.1 通用性重排 / 3.6.2 候选 13 数学栈 / 3.6.3 候选 12 形态识别含实现与三层验证方案 + 动手前必定的三件事）。
  - [x] SubTask 5.7: 新增 §1.4「`src/` 的定位」，把「同仓统一版本管理」澄清为**源码级统一**，并指出引擎下沉可达成**运行时级统一**。
  - [x] SubTask 5.8: §6.1 / 6.2 / 6.3 / 6.4 同步；阶段表由 3 阶段扩为 4 阶段（阶段 2 为形态识别落地，剥离评估后移为阶段 3）。
  - [x] SubTask 5.9: 用交付物正文重建 `spec.md`（保留 `## What Changes` / `## Impact` 两节），确保两份逐字同源；复核 mermaid 仍为 2 张。

# Task Dependencies

- Task 2 依赖 Task 1（图在文档里，先落盘再校图）
- Task 3 依赖 Task 1、2
- Task 4 依赖 Task 3（事实性核对是 Task 3.1 的产出）
- Task 5 依赖 Task 1~4（在已修正版本上做 v2 修订）
- 五个任务均为文档工作，**不触发设备构建与安装**（本任务不产生可运行产物）
