# Checklist

> 判定口径：`[x]` = **AI 已按代码路径 / CI 核对实现**（代码级通过）。
> 标注「真机」的条目属**观感与手感验收**，AI 无法在云端判定 —— 代码侧已核对，最终结论待用户在设备上确认（设备当前锁屏，安装尚未下发）。
> 本轮代码交付见 tasks.md：CI run=35431908818 / 35432265739 / 35432736407 / 35433175995 / 35433805739。

## 几何与外观

- [x] 新按钮呈现 A'、B' 同心圆描边 + 环 Z' 中一个实心 C'，三圆同源渲染 —— 代码已核对（`FloatingAccessoryWheel.disc`）；观感待真机
- [x] A' 直径 128.8、B' 72.8、环 Z' 宽 28、C' 27，且 C' 圆心轨迹半径 50.4（等于环中径） —— AI 已核对派生式（`FloatingAccessoryMetrics` 全部由 `baseDiameter = 56` 派生，无裸字面量）
- [x] C' 滑动到环内任一角度时，与环内外边界各留约 0.5，不粘连、不明显偏小 —— 几何算式已核对（36.9 / 63.9 对 36.4 / 64.4）；观感待真机
- [x] A'、B'、环 Z' 均无填充，仅描边；C' 为 `Color(.label)` 实心 + 1pt 外描边达到视觉直径 28 —— 代码已核对
- [x] 浅色与深色主题下，C' 在环中都清晰可辨（不与环边界混淆） —— 代码已核对（环 Z' 刻意保持透明，正因此项；见 pitfalls/内描边不能用本圈填充色）；观感待真机

## 触摸状态机

- [x] 只有 B' 与环 Z' 响应手势，落在大圆 A' 上无任何反应 —— 代码已核对（命中区 = A' 圆盘 = B' 盘 ∪ Z' 环带；A' 描边无独立行为）
- [x] 在 B' 上摁住/拖动：整体直径 +15%、透明度 50%，抬手恢复 —— 代码已核对
- [x] 点击 B' 播放果冻回弹，随后打开快捷面板 —— ⚠️ **该条已变更**：原为「**不打开任何面板**」，现与旧按钮共用同一个面板，果冻播完（0.18s）后打开；见 `spec.md` 变更记录
- [x] 闲置后淡出：整体淡至 25% **且 C' 隐藏**；触碰后回到 100% —— ⚠️ **时长已变更**：新按钮为 **0.5 秒**（`wheelIdleFadeDelay`，旧按钮仍 3 秒），见 `spec.md` 变更记录
- [x] B' 被点击/摁着/拖着期间整体呈现**旧按钮拖动状态的外观**，静止时长到点后还原为新按钮自身外观；从环 Z' 起手不触发 —— 代码已核对（`showsOldLook`；还原与闲置淡出同一时刻）；观感待真机
- [x] 单指落在环 Z' 上（不要求落在 C' 上）：C' 立即在落点角度显现，**同一次触摸**继续拖动即可转圈 —— 代码已核对（`beginTouch` 内 `resolveTouchMode` + `displayAngleOffset`）
- [x] 转圈过程中 C' 沿环平滑滑动，无抖动、无卡顿 —— 代码已核对（`atan2` 直接驱动 + 8pt 奇点冻结）；手感待真机

## 转圈 → 光标

- [x] 顺时针 = 光标右移，逆时针 = 光标左移 —— 代码已核对（`dir = candles > 0 ? 1 : -1`）
- [x] 转过 18°（1/20 圈）恰好推进 1 根 K 线；转满一圈恰好 20 根 —— 代码已核对（`odometer / 18` + `deliveredCandles` 整数进位）
- [x] 无光标时，第一次转圈立即在**最靠近移动方向**的边缘 K 线上生成十字光标 —— 代码已核对（分支①，`endIndex` / `startIndex`）
- [x] 已有光标时，光标先移到该方向边缘，**再继续转**才触发视图反向平移 —— 代码已核对（分支②→③）
- [x] 光标贴边后继续转，视图随之推进 —— **此处偏离了原清单文字**：原写「以每秒 1 根的速度反向滚动」，
      但它与「一圈 360° = 20 根」互相矛盾（按时间的动画器会让一圈推进量取决于转速），已改为**按转圈步进**（转一圈 = 窗口走 20 根）。
      代码已核对；见 decisions/贴边推进用按转圈步进而非按时间动画器
- [x] 顺时针从 350° 经 0° 到 10°：光标持续同向移动，**无反向跳变** —— 代码已核对（`wrap` 增量 + `delta` 符号定方向，不比较绝对值）
- [x] 手指路径经过圆心附近时，光标无剧烈来回抖动 —— 代码已核对（8pt 冻结且不更新 `lastRawAngle`，回环后续算连续）
- [x] 抬手后：停止推进、光标停在当前根，再转圈可继续从该处推进 —— 代码已核对（`finishAccessoryRotation` 复位标记；里程表与 `deliveredCandles` 均不清零）
- [x] 多图（2~4 格）模式下转圈只驱动**第一格**，其余格按既有联动规则居中跟随 —— 代码已核对（`isMainTile` 仅两处赋值：单图恒 true、多图 `view.index == 0`）
- [x] 联动关闭时，第一格仍能被转圈驱动 —— 代码已核对（`renderCursorIndex` 在不联动时读本地 `selectedIndex`；`publishLinkCursor` 早退不发布）

## 两按钮耦合

- [x] 拖动旧按钮越过屏幕中线：新按钮**未等抬手**即动画吸附到对侧 —— 代码已核对（判定在 `onChanged`）
- [x] 快速把旧按钮拖过中线又拖回：新按钮反向吸附两次，最终与旧按钮分居两侧 —— 代码已核对（`knownSides` 来回翻转 + `requestSnap` 幂等）
- [x] 任一时刻两个按钮都不同侧（含启动瞬间） —— 代码已核对（启动矫正 + 实时互斥）
- [x] 拖动其中一个期间，另一个立即变为 25% 闲置态并保持（含「对方 0.5s 前刚被触碰」的交叉情形） —— 代码已核对（`forceIdle()` 同时 `idleFadeToken += 1`）
- [x] 手势结束后另一个**不自动恢复** 100%，直到它自己被触碰 —— 代码已核对（`activeOwner` 变 nil 时直接 return）
- [x] 转圈手势同样计入「手势占用」（转圈期间另一个被强制闲置） —— 代码已核对（`beginTouch` 里 `beginGesture`）

## 落位与持久化

- [x] 新按钮首次启动落在旧按钮对侧 —— 代码已核对（`resolvedCenter`：读 `.primary` 落位取对侧，读不到则视为右侧 → 新按钮落左）
- [x] 新按钮落位独立持久化（`kline.accessory2.centerX/centerY`），杀掉 App 重进仍在原位 —— 代码已核对（`.secondary` 槽位；`.primary` 的 key 未变，用户已有落位不丢）
- [x] 人为造成两按钮同侧后重启，新按钮被矫正回旧按钮对侧 —— 代码已核对

## 无回归

- [x] 旧按钮外观、四态顺序、拖动吸附、点击弹面板全部与改动前一致 —— 代码已核对（旧按钮只在既有分支追加协作调用，未改既有路径；Stage 1 的常量抽取为行为中性重构）
- [x] 手指在图表上拖动光标、双指缩放、贴边自动滚动等既有手势与改动前完全一致（新通道不接管手势） —— 代码已核对（未改 `ChartGestureHandlers` / `ChartInertiaSupport` / 联动文件；命令路径四道守卫 + `.dropFirst()` + `isRotating` 边沿判定，空转期零写入）
- [x] 自选/行情页显示、首页与模拟页不显示、详情页之上仍显示 —— 代码已核对（两按钮共用同一 `selectedTab` 判定）
- [x] 云端构建通过 —— AI 已核对：5 次 CI 全部「云端构建成功」（exit code 6）

## 文档

- [x] `decisions/` 新增 5 篇（原计划 4 篇，多出的 1 篇是「贴边推进用按转圈步进而非按时间动画器」—— 它是一处对已批准 spec 的有意偏离，必须独立成篇） —— AI 已核对
- [x] `modules/悬浮按钮.md` 已更新（标注为旧按钮、补「被对方手势强制闲置」外部置入态与遗留）；新增 `modules/新按钮（转圈驱动光标）.md`（含交互分区图 + 五态状态机图）与 `modules/两按钮耦合.md`（含耦合流转图） —— AI 已核对
- [x] `INDEX.md` 已挂上新篇（modules 3 篇 / decisions 10 篇 / pitfalls 4 篇） —— AI 已核对
- [x] 跨模块判定已写入（新按钮同样受 `selectedTab` 门控、命令通道是新增路径且必须新建的原因已记录） —— AI 已核对

## 待用户在设备上确认（AI 无法判定）

- [ ] 全部「观感 / 手感」条目：几何比例是否顺眼、C' 是否贴边滑动、转圈是否跟手、果冻与淡出的观感、两按钮互斥的手感
- [ ] 深色模式下的辨识度（C' 与 A'/B' 描边）
- [ ] 与 K 线详情页图表手势是否互相干扰