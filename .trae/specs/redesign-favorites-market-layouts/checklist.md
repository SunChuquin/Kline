# Checklist

> 核验方式：逐条对照实现代码（静态核验，文件 + 行号）与原型文件。标 ⏳ 的为纯视觉/交互项，需真机确认。

## figma 原型画廊

- [ ] `figma/favorites-market-ui-proposals.html` 为自包含单文件，无外链依赖（无 CDN / 无远程字体 / 无远程图片）
- [ ] 设备框与既有画廊一致（iPad mini 4 横屏 1024×768，等比缩放至 573×430，`.bezel` 深色外壳）
- [ ] 自选页 4 屏齐全且可切换：A 经典表格式（照现状复刻）/ B 分组侧栏 + 表格工作区 / C 自选卡片流 / D 分组看板 + 紧凑表格
- [ ] 行情页 4 屏齐全且可切换：A 经典表格式（照现状复刻）/ B 分类侧栏 + 表格 / C 磁贴卡片网格 / D 概览 + 紧凑表格
- [ ] 个人中心演示屏展示五行设置行（主题 / 快捷面板 / 模拟页 / 自选页 / 行情页）与四选项选择面板
- [ ] 每屏有编号标注，且与右侧标注栏条目一一对应（描述 / 布局标注 / 优势 / 代价）
- [ ] 跨方案对比表覆盖 8 档，维度含信息密度 / 横屏利用 / 首屏信息量 / 操作步数 / 实现复杂度 / 适配场景
- [ ] 支持 `#shot=<屏 id>` 单屏定位，无头浏览器截图时无多余区块干扰
- [ ] 逐屏截图已输出到 `figma/_shots/`（`fav_A`…`mkt_D`、`profile_layouts`），每屏无溢出 / 无文字截断 / 无元素重叠

## 布局偏好与个人中心

- [ ] `PageLayoutStore` 为单例 `ObservableObject`，两个布局字段写入 UserDefaults（key `kline.favoritesLayout` / `kline.marketLayout`），默认值均为 A
- [ ] 存储值非法或缺失时回退 A 且不崩溃
- [ ] 个人中心出现两行新设置「自选页布局」「行情页布局」，右侧下拉按钮显示当前档名（字号 12 / 高 28 / `.plain` / 蓝色前景）
- [ ] 两个新选择面板样式与 `KlineThemeOptionsPanel` 逐项一致（210pt 宽、行内 padding、蓝色 checkmark、底部「完成」、圆角 12、阴影 black 20% radius 12 y 4）
- [ ] 两个新浮层挂在页面容器层 overlay（`black 25%` 遮罩 + 居中 + `.transition(.opacity)` + `zIndex(1000)`），不被 ScrollView 裁剪
- [ ] 六个浮层（主题 / 快捷面板 / 模拟页 / 自选页 / 行情页 / 公式中心）互斥，同时只呈现一个
- [ ] 泛型组件改名后既有两行（快捷面板 / 模拟页）样式与交互无变化
- [ ] 切换布局无需重启即生效；重启后仍保持所选档位

## 自选页四档

- [ ] `FavoritesView` 按 `favoritesLayout` 分发 A/B/C/D 四档
- [ ] A 档即改造前实现（工具条 + 分组 Tab 条 + 吸顶表头 + 表格行 + 长按菜单 + 下拉刷新 + 编辑模式重排），`favorites.title` 标识保留
- [ ] B 档：216pt 分组侧栏（图标 + 名称 + 数量、选中高亮、底部「新建分组 / 管理分组」、公式分组刷新入口）与右侧工作区联动
- [ ] C 档：卡片式每标的 72pt 卡片（名称/代码、迷你走势、现价、涨跌幅胶囊），「卡片 / 表格」可切换，切到表格式后与 A 档能力一致
- [ ] C 档：下拉刷新、长按菜单（取消自选 / 加入分组 / 移动分组）、编辑模式重排（`List` + `onMove`）与 A 档行为一致
- [ ] D 档：分组看板卡（组名 / 标的数 / 涨跌家数 / 组内平均涨跌幅）与统计条数值与该分组实际数据一致
- [ ] D 档：紧凑表格行高 34、字号 15，表头吸顶、冻结列横向滚动正常
- [ ] 四档共用同一套 `FavoritesStore` / `MarketConfigStore` / `MarketRowCache`，切换布局不丢失当前分组、排序与筛选配置
- [ ] 四档的表头设置、冻结列横向滚动、边线拖改、表头排序、字段筛选、长按菜单、下拉刷新、空态、加载态均可用
- [ ] 四档均使用语义化颜色，深色模式可读（无写死白底）
- [ ] 可点击元素命中区 ≥ 44×44pt；行高固定，状态切换不抖动 ⏳

## 行情页四档

- [ ] `MarketView` 按 `marketLayout` 分发 A/B/C/D 四档
- [ ] A 档即改造前实现（一级菜单 + 上下箭头图标 + 二级胶囊 + 左设置 / 右公式、搜索 + 表格），`market.topMenu.*` 与 `market.rowCard` 标识保留
- [ ] B 档：200pt 分类侧栏（一级分区 + 二级条目 + 数量角标、选中高亮），点条目右侧表格即时切换
- [ ] C 档：磁贴列数按可用宽度自适应（≥960 三列 / ≥640 两列 / 否则一列），每格带 `market.rowCard` 标识，点击按当前分类上下文打开 K 线详情
- [ ] C 档：磁贴右上角自选星标可切换加/取消自选；「磁贴 / 表格」可切换，切到表格式后与 A 档能力一致
- [ ] D 档：概览条涨/跌/平家数、涨停/跌停数、总成交额与当前分类实际数据一致，且统计在快照阶段预计算（`body` 内无全表重计算）
- [ ] D 档：紧凑表格行高 34、字号 15，二级胶囊与工具条保留
- [ ] 四档共用同一套分类、排序与筛选配置，切换布局不丢失当前一级 / 二级分类与排序筛选
- [ ] 四档的表头设置、边线调节、冻结列横向滚动、排序、字段筛选、长按菜单、下拉刷新、搜索页、公式入口、空态、加载态均可用
- [ ] 四档均使用语义化颜色，深色模式可读（无写死白底）
- [ ] 可点击元素命中区 ≥ 44×44pt；行高固定，状态切换不抖动 ⏳

## 共享骨架与数据

- [ ] `FavoritesPageKit.swift` / `MarketPageKit.swift` 承载工具条、导航、表格主体、卡片 / 磁贴、概览条与全部 sheet / overlay，各档布局视图只做组合，无业务逻辑复制
- [ ] 布局拆分过程遵循 `.trae/skills/swiftui-large-file-split`（recon → 计划 → 执行 → 验证）
- [ ] `MarketRow.recentCloses` 为只读派生访问器，既有字段、缓存与格式化行为无变化
- [ ] 未新增数据源、未改任何持久化文件结构（`favorites.json` / `market_columns.json` / `sim.json` 不变）

## 工程与交付

- [ ] 新增文件全部位于 `Kline/` 目录树内，无需手工改 `project.pbxproj`（依赖 `PBXFileSystemSynchronizedRootGroup`）
- [ ] 阶段二 / 三 / 四闭环命令均返回 0 / 6 / 7，构建通过
- [ ] 交付说明包含各阶段 build 号、8 档一览与真机验证路径；`git status` 无遗留未提交改动
